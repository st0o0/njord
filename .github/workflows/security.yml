name: Security

on:
  pull_request:
    paths:
      - 'Dockerfile'
      - 'src/Directory.Packages.props'
      - 'src/**/*.csproj'
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch:

concurrency:
  group: security-${{ github.ref }}
  cancel-in-progress: true

jobs:
  scan:
    uses: st0o0/github-workflows/.github/workflows/security.yml@main
    with:
      docker-image-name: njord
    permissions:
      contents: read
      security-events: write
